# ==============================================
# PHASE 1: DATABASE CONFIGURATION (Vercel Postgres)
# ==============================================
# These are automatically populated when you pull envs from Vercel
POSTGRES_PRISMA_URL="postgres://default:password@ep-url.us-east-1.aws.neon.tech/verceldb?sslmode=require&pgbouncer=true"
POSTGRES_URL_NON_POOLING="postgres://default:password@ep-url.us-east-1.aws.neon.tech/verceldb?sslmode=require"
DATABASE_URL="postgres://default:password@ep-url.us-east-1.aws.neon.tech/verceldb?sslmode=require"

# ==============================================
# PHASE 2: AUTHENTICATION (NextAuth.js)
# ==============================================
# Application URL
# Local Development: http://localhost:3000
# Production: https://your-vercel-domain.vercel.app
NEXTAUTH_URL="http://localhost:3000"

# Critical for Security (prevents 500 errors)
# Generate with: openssl rand -base64 32
# IMPORTANT: Set both variables to the same value for maximum compatibility
AUTH_SECRET="change_me_to_a_random_string"
NEXTAUTH_SECRET="change_me_to_a_random_string"

# Google OAuth Credentials (for Teacher Login)
# Obtain these from Google Cloud Console -> APIs & Services -> Credentials
# SETUP INSTRUCTIONS:
# 1. Go to https://console.cloud.google.com/apis/credentials
# 2. Create OAuth 2.0 Client ID (Application type: Web application)
# 3. Add Authorized redirect URIs:
#    - http://localhost:3000/api/auth/callback/google (for local development)
#    - https://your-vercel-domain.vercel.app/api/auth/callback/google (for production)
# 4. Copy Client ID and Client Secret below
GOOGLE_CLIENT_ID="your-google-client-id"
GOOGLE_CLIENT_SECRET="your-google-client-secret"

# ==============================================
# PHASE 4: AI ITEM GENERATION (Anthropic Claude)
# ==============================================
# Anthropic API Key for generating assessment items
# Obtain from: https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY="your-anthropic-api-key-here"

# ==============================================
# PHASE 5: FEATURE FLAGS & CONFIG
# ==============================================
# Toggle to enable/disable specific features without redeploying
NEXT_PUBLIC_ENABLE_CAT_ALGORITHM="true"
NEXT_PUBLIC_SCHOOL_YEAR="2025-2026"
